<Window x:Class="CarnetSante.WPF.Views.Auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CarnetSanté - Connexion"
        Width="480" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F0F4F8">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="180"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête bleu -->
        <Border Grid.Row="0" Background="#1A3A5C" CornerRadius="0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🏥" FontSize="48" HorizontalAlignment="Center" Margin="0,10,0,5"/>
                <TextBlock Text="CARNET DE SANTÉ SANITAIRE" FontSize="16"
                           FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="BURKINA FASO" FontSize="13"
                           Foreground="#A8C8E8" HorizontalAlignment="Center" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <!-- Formulaire connexion -->
        <Border Grid.Row="1" Background="White" Margin="40,20,40,40"
                CornerRadius="10" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.15"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Connexion" FontSize="20" FontWeight="Bold"
                           Foreground="#1A3A5C" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <!-- Login -->
                <TextBlock Text="Identifiant" FontSize="12" Foreground="#555" Margin="0,0,0,5"/>
                <TextBox x:Name="LoginBox"
                         Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14" Padding="10,8" Margin="0,0,0,15"
                         BorderBrush="#CBD5E0" BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#1A3A5C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Mot de passe -->
                <TextBlock Text="Mot de passe" FontSize="12" Foreground="#555" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox"
                             FontSize="14" Padding="10,8" Margin="0,0,0,20"
                             BorderBrush="#CBD5E0" BorderThickness="1"/>

                <!-- Message d'erreur -->
                <Border Background="#FEE2E2" CornerRadius="5" Padding="10,8" Margin="0,0,0,15"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626"
                               FontSize="12" TextWrapping="Wrap"/>
                </Border>

                <!-- Bouton Connexion -->
                <Button Content="{Binding IsLoading, Converter={StaticResource LoadingToTextConverter},
                                  ConverterParameter='Connexion en cours...|Se Connecter'}"
                        Command="{Binding ConnecterCommand}"
                        FontSize="14" FontWeight="Bold" Padding="0,12"
                        Background="#1A3A5C" Foreground="White" BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2D5A8E"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#94A3B8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBlock Text="v1.0 - Données confidentielles" FontSize="10"
                           Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
